// script.js

console.log("Le fichier script.js est bien chargé."); // Ajoutez cette ligne pour vérifier si le fichier est chargé

function modifierCategorie(id) {
    console.log("Modifier la catégorie avec l'ID " + id); // Ajoutez cette ligne pour vérifier si la fonction est appelée
    // Ajoutez ici le code pour gérer la modification de la catégorie avec l'ID spécifié
    alert("Modifier la catégorie avec l'ID " + id);
}

function supprimerCategorie(id) {
    console.log("Supprimer la catégorie avec l'ID " + id); // Ajoutez cette ligne pour vérifier si la fonction est appelée
    // Ajoutez ici le code pour gérer la suppression de la catégorie avec l'ID spécifié
    alert("Supprimer la catégorie avec l'ID " + id);
}
